[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "almas-twin-flame-astrology"
version = "1.10.2"
description = "Núcleo público de puntuación y validación para la Astrología Metafísica Relacional ALMAS"
requires-python = ">=3.10"
authors = [{name = "Proyecto ALMAS"}]
readme = "README.md"

[project.scripts]
almas-score = "almas_tfa.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
